name: Label PR
on:
  pull_request:
    types: [opened]

jobs:
  label_regex:
    runs-on: ubuntu-latest
    name: Add label
    steps:
      - name: "Assign label to new Pull Request"
        uses: Bhacaz/label-regex@v1
        with:
          field: body
          regex: '(AUTOMERGE)'
          lowercase: true
          token: ${{ github.token }}
