name: Enable Auto Merge
on:
  pull_request:
    branches:
      - develop

jobs:
  auto-merge:
    runs-on: ubuntu-latest
    steps:
      - uses: kenhowardpdx/auto-merge-action@v1
        with:
          token: ${{ github.token }}
          merge_method: MERGE
          
